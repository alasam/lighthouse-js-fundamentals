const isOdd  = function (num) {
  return num % 2 > 0; // checks to see if number can be divided by 2 and have a remainder greater than 0. If so, the bolean will return true.

console.log("3 is odd: " + isOdd(3));
console.log("8 is odd: " + isOdd(8));
